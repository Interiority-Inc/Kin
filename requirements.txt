# Kin — Python dependencies
#
# TEE handler
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.28.0
pydantic>=2.10.0

# Inference handled by Phala Confidential Inference API (remote, no local deps)

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
